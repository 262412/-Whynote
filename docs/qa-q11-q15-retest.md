# Q-11～Q-15 独立复验记录

日期：2026-09-26。**最新已核查 main `db6d5b1` 已包含 PR #14：Q-11～Q-15 在指定范围局部通过，新增 H-02 边界 Q-16 失败；S0 完整验收及生产仍为 NO-GO。** 本轮不修改业务实现，不代替非作者评审或责任人签署。以下保留旧 main `86f21b7` 失败、候选快照、稳定提交及合并后的证据，最终判定见末节。

## 实际基线与证据等级

- 知因 main：`86f21b72c9864e2774c635b386f06c1fb3d3595e`。PR #9～#12 均已合并；相对上轮 `0844d3e` 只增加测试报告及两份文档状态修订，业务代码未变。main CI [36237636868](https://github.com/262412/-Whynote/actions/runs/36237636868) 成功。
- 飞书首次读取：PRD **68**、技术文档 **12**。FR-01/10/11/13、D-11～D-13 和 Q-11～Q-15 没有新的修订。D-13 要求当前身份、版本、票据及重试绑定，尚未具体定义“新菜单签发即取代旧挂起回调”；技术文档及仓库的旧菜单拒绝要求仍有效。Q-15 不以测试自行签署新的产品语义。
- PR #10/#11/#12 的 GitHub review 只有 COMMENTED，未见 APPROVED；main 查询结果 `protected=false`。CI 成功不等于独立批准或数据/隐私签署。
- 原工作目录的既有修改未纳入本轮。旧测试工作树在初次核查后从磁盘及 `git worktree list` 消失，旧 `var/qa-pr10-11` 脚本无法重读或复用；因此新建 `whynote-qa-retest` 工作树，从 main 重建测试。开发侧旧工作树之后也消失；没有清理或覆盖其审计数据。
- 固定 Open WebUI 上游：`8bd8b4fac5e059578ac0c74b3c18d11139f88b7d`（v0.11.4）。从当时尚在的上游 Git 仓库复制干净检出，核对 HEAD，再正向检查、应用补丁、逆向检查。补丁 SHA-256：`4633b452bad2babebf8b7fc409e8eff2c7d21d1855839079d2281d67d4fda206`。检查执行真实加载的 evaluations 源文件位于独立检出。
- ORM/路由层：每次新建虚构 SQLite，执行上游迁移、真实 ORM、权限分支、查询和删除。仅替换身份依赖与通知。初始 47 项为 39 通过、8 失败；按文档更新补充 H-02 及开关开启矩阵后，最终 56 项为 **47 通过、9 失败**。首次脚本写错两个 GET 路径，已对照路由修正并在新库重跑；那些 404/405 不计为业务缺陷。开关开启分支在路由模块配置值切换为 True 后执行实际查询，随后恢复 False；真实 HTTP 只测关闭配置。
- 完整服务 HTTP 层：在 `127.0.0.1:8092` 启动同一补丁后端，用真实 signup/add/signin 生成三个 `.invalid` 账号；未替换登录、身份、事件通知或数据库。18 项检查为 **12 通过、6 失败**。令牌及随机口令只在测试进程内使用，不保存或提交。
- Action 层：实际 `s0_action.py`、实际 EventStore、独立 SQLite；替换宿主归属查询和菜单回调，以模块私有时钟控制边界，不改 asyncio 时钟。9 项检查 **5 通过、4 失败**；每项同时重开 EventStore 验证事件与投影一致。
- 本轮重新执行 Ruff 全仓 lint、format 和既有 pytest，**46/46 通过**。现有 CI 仅检查 `src/tests`，不运行本报告的显式 QA 探针或外部 Open WebUI 补丁。失败探针保留真实断言和非零退出码，没有 xfail、skip 或降低断言。

## 缺陷逐项复验

| ID / 需求 | 操作与预期 | main 实际与证据 | 状态 / 复验条件 |
| --- | --- | --- | --- |
| Q-11 / P2 / D-13、FR-11 | 墙钟 1000.9、单调时钟 2000.0 签发；在 59.5、59.999 秒返回本菜单有效签名，应仍可提交。60.001 秒应拒绝且无展示。 | 两个小数秒用例均为 `ticket_expired`，仅动作事件；0、59 秒成功，60.001 秒正确拒绝。见 `ticket-results.json` 前五项。 | 开放。需修复完整 60 秒与真实到期边界，再检查签名兼容。 |
| Q-12 / P1 / Q-05、D-12 | 开关关闭；管理员 GET/POST 他人评分应拒绝；拒绝不能回传内容或修改行。 | GET 401，POST 仅 `type=rating` 返回 200 及旧评论；POST 改评论也 200，SQL 行从 `synthetic-comment` 变为 `synthetic-unauthorized-change`。真实登录 HTTP 同样复现，原评论改为 `synthetic-http-mutated`。 | 开放。需覆盖所有评分返回路径和拒绝零副作用；401/404 按明确契约精确核验。 |
| Q-13 / P2 / D-12 | 管理员是评分所有者时，应保留 GET/POST 权限。 | 自己 GET 401、POST 200；真实登录 HTTP 同样复现。 | 开放。所有者查询须优先于跨用户导出开关。 |
| Q-14 / P1 / D-12 | 普通删全部聊天只清理实际删除聊天关联评分；无 chat_id、其他聊天评分保留；删账号才清该用户全部评分。 | 无 chat_id 的合法评分被删；该用户关联另一用户仍存在聊天的评分也被删；用户无任何聊天时，无关联评分仍被删。真实 HTTP `DELETE /api/v1/chats/` 返回 200，SQL 确认该评分消失。 | 开放。须分开聊天与账号删除范围，回归空聊天集、其他聊天、文件夹和账号。 |
| Q-15 / P2 / D-13、FR-11/13 | 同用户/会话/对象 A 先打开、B 后打开；先取消 B，再释放 A 的有效回复。旧 A 应拒绝且无追加。 | 同 Action 与两个 Action 实例均接受 A；事件为动作 → B 展示 → A 展示 → A 选因。若 B 已选择，A 被现有状态规则拒绝且无追加，此对照通过，不能掩盖取消分支失败。 | 开放。需明确并实现签发次序失效，覆盖取消/断线/超时/挂起及展示到选因间的竞争。 |

### 关闭开关时 GET/POST 权限矩阵

| 主体 / 目标 | 预期 GET / POST | ORM 路由实测 | 真实登录 HTTP |
| --- | --- | --- | --- |
| 普通用户 / 自己 | 200 / 200 | 200 / 200 | 200 / 200 |
| 普通用户 / 他人 | 404 / 404 | 404 / 404、无写入/内容泄漏 | 404 / 404、无内容泄漏 |
| 管理员 / 他人 | 401 / 401（本轮 main 契约） | 401 / **200** | 401 / **200** |
| 管理员 / 自己 | 200 / 200 | **401** / 200 | **401** / 200 |
| 普通用户 / 管理员 | 404 / 404 | 404 / 404、无写入/内容泄漏 | 未另测 |
| 未认证 / 普通用户 | 401 / 未测 | 未另测 | 401 / 未测 |

## √ 本轮通过的具体子项

- √ D-12 新评分 snapshot 持久化为 JSON null；创建/更新非空 snapshot 均 422，数据库不变。管理员 `/feedbacks/all/export`、`/feedbacks/list`、`/feedbacks/all/ids` 均 401。
- √ D-12 越权单条删聊天返回 False 且评分不变；批量删除的关联评分被清除、其他用户聊天及其评分保留。文件夹删除保留其他文件夹及无关联评分。真实账号删除返回 True，用户、聊天、关联/无关联评分均清除，其他用户评分保留。
- √ 活动异步连接 `secure_delete=1`、`journal_mode=delete`。不证明旧库、备份、日志、导出或物理副本已擦除。
- √ FR-11/13 既有选择/更正、来源、篡改/跨会话重放拒绝、撤权/版本变化拒绝及历史回执用例继续通过。新增票据用例重开数据库后的事件与投影一致；这不构成新票据表迁移验收。
- √ 真实服务 HTTP 版本为 0.11.4；真实登录后的普通所有者与普通他人读取/更新矩阵符合上述具体预期；未认证 GET 为 401。

已知 NameError 自动评审意见不列缺陷：固定上游 ChatMessage 删除使用独立 select，本轮真实账号删除继续返回 True。

## 浏览器与环境限制

本轮通过 Computer Use 建立了 in-app browser 标签，但打开 `http://127.0.0.1:8092` 被 `net::ERR_BLOCKED_BY_CLIENT` 阻止。没有形成新的浏览器评分、签名菜单或前端网络证据；未重建前端。开发旧目录的前端构建也在本轮期间失去可用性，最终可重复启动器明确只服务 HTTP API。上轮开发浏览器证据仍仅作为历史，不记为本轮独立 E2E。

测试只使用新建虚构库。未调用真实推断；未批准生产上下文、原生评分复用、公开/真实用户数据、auto-attach 或自由文本 SLM。离线配置不等于环境级网络阻断验收，完整出站与生命周期审计仍缺。

## 重跑方法与可持久证据

本报告使用的脚本纳入 `qa/`，JSON 证据位于 [`qa/evidence/2026-09-26-q11-q15`](../qa/evidence/2026-09-26-q11-q15)。JSON 只保留虚构行、事件、状态及结果，不含认证信息。运行日志、完整数据库和飞书读取快照留在工作树忽略目录 `var/qa-q11-q15/`。

```powershell
uv sync --extra dev --locked --no-editable
uv run --no-sync ruff check .
uv run --no-sync ruff format --check .
uv run --no-sync pytest -q
uv run --no-sync pytest qa/test_s0_regressions.py -q -s
```

外部补丁按 [固定上游方法](openwebui-native-s0-patch.md) 在新检出应用。使用有 `open-webui==0.11.4` 的 Python 环境（本轮 Python 3.11.14；依赖版本见 native-results.json）：

```powershell
# 将 $qaPython 指向固定 0.11.4 环境的 python.exe；路径本身不包含凭据。
& $qaPython qa/native_regressions.py --source var/qa-openwebui-v0114 --output var/native-new
# 此进程持续运行，所有数据目录必须全新；在另一终端执行 HTTP 检查。
& $qaPython qa/serve_openwebui.py --source var/qa-openwebui-v0114 --data-dir var/http-new
uv run --no-sync python qa/http_regressions.py --data-dir var/http-new
```

原生与 HTTP 探针默认精确断言 main 的跨用户 401。若修复契约明确改为 404，使用 `--admin-other-status 404`，并在报告注明对应提交/契约；不得改为任意非 200 来放宽断言。拒绝无泄漏、无写入以及本人 200 的断言保持不变。

## 责任与下一次门槛

Q-11/Q-15：客户端、后端与契约责任人；Q-12～Q-14：宿主补丁维护者及数据/隐私负责人。具体签署人均待指定/确认。本报告仅记录独立执行结果，不冒充非作者 APPROVED 或产品/数据批准。

修复分支出现后，必须按稳定提交独立应用补丁，重跑上述探针、核心回归、事件重放/幂等及新增表兼容，并补可运行浏览器的受影响路径。完整 FR、D-12/D-13、真实数据和上线在这些条件及责任人签署完成前继续 NO-GO。

## 本轮追加：最新文档与候选修复快照

写入飞书前再次读取：PRD 从 68 更新到 **76**，技术文档从 12 更新到 **15**。变化已按编号核对：

- FR-03 更正为门控拒绝不读取推断上下文、不调用模型；FR-12 按 D-14 统一为 P2、默认关闭。本轮没有执行模型能力测试或批准新数据范围。
- D-15 明确同主体/目标版本/event_id 的新有效菜单签发取代旧菜单，取消/断线/超时不恢复；签发不是展示，身份校验失败的新请求不得使旧菜单失效。Q-15 现在有明确产品规格，API 顺序与兼容仍待责任人签署。
- H-01 要求 GET/POST 本人/他人及开关双状态；H-02 要求可信对象关联、禁止只凭客户端 chat_id 跨主体删除；H-03/04 要求完整时效、并发及重启；H-05/06 的完整宿主范围、状态字典与用户研究仍未验收。
- 技术文档头部已引用 PRD 76，D-11～D-13 加入修订 68 的历史引用已纠正；没有把这些文档修正写成业务代码修复。

开发侧 `codex/s0-review-fixes` 在复验时尚无新提交。为避免测试过程中源文件变化，在第二个隔离工作树 `whynote-qa-candidate` 固定捕获了相对 `86f21b7` 的 diff（SHA-256 `20334c87fd93b9e99f8cadaf4f94926e86425bc9847d448a61f1a382f69884a4`）。测试对象是这个不可变采样快照，不能以 main SHA 冒称修复已提交。关键文件 SHA-256：

| 文件 | 快照 SHA-256 |
| --- | --- |
| s0_action.py | `825255b570335a8768a57934927139d12d7231fc2d01ad7c615ca54b90cda11f` |
| store.py | `573d630383141bc74d294a1eb3f3e869d91fc174ef46d0f8f29197a1539d8e9d` |
| native-v0.11.4-s0.patch | `674699c7cb03bc23abcb734b672d0bc32ac334e22f6bb4f397dd4b05f3fb30ff` |

候选契约见该工作树 `docs/s0-review-fixes.md`；对应读取快照哈希及测试文件哈希保存在 `candidate-results.json`。管理员跨用户 GET/POST 关闭时的预期从 main 契约 401 改为候选明确约定的 **404**，因此使用 `--admin-other-status 404`。本人 200、禁止泄漏和禁止写入断言未放宽。

### √ 候选快照的局部修复验证

- √ Q-11：59.5、59.999 秒均可提交，60.001 秒无展示/原因。候选核心测试还覆盖刚到 60 秒、墙钟跳变、等待取消。独立票据/并发探针 **9/9**，候选核心 pytest **64/64**，相关 Ruff lint/format 通过。
- √ Q-15：B 取消后 A 的回调在同实例和不同实例均被拒绝，无新增展示/原因；B 已选择的对照仍通过。候选核心回归覆盖 B 断线/挂起、展示到选因间票据替代等。未覆盖真实浏览器竞态或多进程压测，不升级为 D-15 整体验收。
- √ Q-12/Q-13：候选实际补丁按原生 47 项旧范围全通过；管理员自己 GET/POST 200、他人 GET/POST 404，提交改评论被拒且 SQL 行不变。真实 signup/add/signin 后的 HTTP **18/18** 通过，排除身份替换影响。开启配置时四组 GET/POST 的 8 项补充矩阵也通过，管理员他人读写 200、普通他人 404。
- √ Q-14 既有复现范围：无关联、其他聊天及空聊天集的评分保留；所删聊天关联评分删除，文件夹/账号边界仍通过。此 √ 不包含下面 Q-16 的伪关联。
- √ 实际旧库升级：由 main 的旧 EventStore 建库（确认没有 display_tickets），用候选 EventStore 打开。所有旧表原始行和投影不变；签发不写事件/Outbox；旧已完成选择的幂等重试不追加；旧展示回执为 historical/actionable=false；最新票据重开后仍在。6 项检查见 `upgrade-results.json`，脚本 `qa/store_upgrade.py`。

### Q-16 / P1：客户端伪关联可触发跨用户评分清理（H-02）

**操作：** Alice 有聊天 a-chat，Bob 有独立聊天 b-chat。以 Bob 的真实评分路由主体 POST `/feedback`，提交 `meta.chat_id=a-chat`；接口接受 200，但没有验证 Bob 对 Alice 聊天的归属。随后调用 Alice 的 `Chats.delete_chats_by_user_id`。

**预期：** 按技术文档 15 H-02，不能仅凭这条客户端关联删除 Bob 的评分。应在写入时拒绝/建立可信关联，或在清理时保留未经验证的外部主体记录；具体状态码、历史数据处理与授权关系由后端/数据责任人冻结。

**实际：** main 与候选均删除该 Bob 评分；Bob 的 b-chat 及其合法关联评分仍在。`Q16-untrusted-foreign-rating-preserved` 实际 false、预期 true。候选最终扩展原生结果 **55 通过、1 失败**；main **47 通过、9 失败**。真实 ORM/评分路由复现，数据库记录创建 200 后被清理；未把该项称为新的浏览器或真实登录 HTTP 证据。它是 H-02 新增约束下的数据范围缺口，不能用“与评分作者无关”的候选条款自动覆盖产品规格。

Q-16 开放；责任角色为宿主补丁维护者、后端、数据/隐私（具名签署待指派）。下一次须同时覆盖：自己/外人/不存在的 chat_id 的创建与更新、单条/批量/文件夹/账号清理及回滚、旧数据兼容。Q-11～Q-15 仅记为“指定候选快照局部通过”，仍待稳定修复提交比对、CI、浏览器及非作者/责任人签署；main 未合入时不得关闭 main 缺陷。

## 最终追加：PR #14 稳定提交与发布判定

[PR #14](https://github.com/262412/-Whynote/pull/14) 随后提交 `9b990cd77c6907bac326ffeb52ea477f00302eba`。已逐文件比对 Git blob：`s0_action.py`、`store.py`、原生补丁与上述被测快照一致（统一 CRLF/LF 后比较）；`tests/test_feedback.py` 一致，Action 测试及说明文档有后续变化。因此保留最初快照记录，并切换隔离候选工作树至该准确提交重新执行：相关 Ruff lint/format 通过、核心 **64/64**、独立票据 **9/9**。补丁业务文件未变，原生扩展 **55/56**、真实 HTTP **18/18** 及旧库升级六项证据适用于该提交；唯一失败仍为 Q-16。

PR #14 [CI 36239016299](https://github.com/262412/-Whynote/actions/runs/36239016299) 的 quality 与 native-regression 均成功；实际日志为核心 64/64、原生回归 24/24。已核对 workflow 确实检出固定上游、校验 SHA、应用补丁并运行原生测试，之前“CI 不执行外部补丁”的缺口在这个 PR 中得到补充。该 CI 尚未包含 Q-16 探针，不能据绿色 CI 将 H-02 整项判为通过。PR #14 描述所述浏览器与构建属于开发侧本轮证据，测试侧仍未独立完成浏览器 E2E。

main 仍为 `86f21b7`，PR #14 尚未合入。测试 PR #13 不合并该业务实现。结论：**Q-11～Q-15 在 `9b990cd` 指定测试范围局部通过；Q-16/H-02 不通过；完整验收 NO-GO。** 待 Q-16 关联契约及修复、相关前端/浏览器、非作者批准和责任人签署后再复验；main 合并后还须确认实际部署版本。旧快照、备份/导出、物理副本和环境出站审计未关闭。

飞书本轮通过准确修订号追加到 [PRD QA-20260926-R2](https://my.feishu.cn/wiki/XG6SwutL0i3fyWkwmhScEEB86Gg#doxc6DaZzx8QmzsAHhi0ugBslth)（76→77）及 [技术文档 QA-20260926-R2](https://my.feishu.cn/wiki/GuphweJs3iWwBRkBDjlcljA16bh#doxc6ruKcn7lcLrEHH2vsYL2EXc)（15→16）；读回验证历史前缀完整保留、计数/Q-16/NO-GO 正确。稳定提交信息再追加到 [PRD 修订 78](https://my.feishu.cn/wiki/XG6SwutL0i3fyWkwmhScEEB86Gg#doxc6ua8E0FkxnRJIQplUGu052e)和 [技术文档修订 17](https://my.feishu.cn/wiki/GuphweJs3iWwBRkBDjlcljA16bh#doxc67tDE0CUiyB6rdpsO2JLBVc)，读回确认提交、55/56、Q-16/H-02 及 NO-GO 均正确。原目录 git status 与开工时一致；本轮 8092 测试服务已停，数据库保留供复核。

## 收尾追加：修复已合入 main，Q-16 仍开放

收尾核查时，其他流程已将 PR #14 合并为 `49e5ff8`，并把测试 PR #13 的首个提交 `67d0eeb` 合并为最新 main **`db6d5b19f365e3e739bec7d0fc4bce2c4994e122`**。本测试任务没有执行这些合并。PR #13 关闭后，后续测试提交 `d78af78` 尚未随其进入 main，因此通过新的独立测试分支 `codex/qa-q16-main-review` 提交补充报告和证据，不把已关闭 PR 当作最新内容已经合入。

已将 main 合入测试分支，确认 main 的 Action、EventStore、原生补丁与 `9b990cd` 完全相同；重新安装本地 Whynote 包后执行 Ruff 全仓检查及核心 **64/64**、独立票据 **9/9**，均通过。原生 **55/56**、真实 HTTP **18/18**、旧库升级六项结果对应相同业务代码。main [CI 36239322146](https://github.com/262412/-Whynote/actions/runs/36239322146) 成功；PR #14 review 仍只见 COMMENTED，合并没有补齐独立批准或责任人签署。

**最终状态：Q-11～Q-15 在合并后 main 的上述范围局部通过；Q-16/H-02 开放，完整验收仍为 NO-GO。** 先前“尚未提交/尚未合并/main 缺陷未关闭”的段落仅描述当时状态，不覆盖本节的收尾事实。
# 后续独立复验

2026-09-26：PR #16 已合入 main `57f09ce`。Q-16 的新写入、历史保留、迁移和事务子项已独立复验通过；Q-11～Q-15 回归仍通过。详见 [Q-16 本轮验收记录](qa-q16-acceptance.md)。本报告下文保留上一轮失败与当时状态；新的技术通过不替代完整 FR、独立浏览器、非作者批准和测试结果签署。
